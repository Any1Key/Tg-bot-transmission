services:
  bot:
    build: .
    container_name: transmission-telegram-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - bot_data:/app/data
      - ./config.yml:/app/config.yml:ro

volumes:
  bot_data:
